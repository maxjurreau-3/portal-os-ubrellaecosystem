{
  "name": "portal-os",
  "version": "3.0.0",
  "description": "Portal‑OS v3 multi‑engine runtime",
  "type": "module",
  "main": "src/index.jsx",
  "scripts": {
    "start": "cd public && vite",
    "build": "cd public && vite build --outDir ../dist",
    "preview": "cd public && vite preview --port 5000"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
